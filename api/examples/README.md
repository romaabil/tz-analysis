# Примеры API для проекта "Петрушка Зеленая"

Эта папка содержит примеры запросов и ответов API для мобильного приложения.

## 🗂️ Структура

- `requests/` — примеры HTTP-запросов
- `responses/` — примеры JSON-ответов
- `schemas/` — JSON-схемы для валидации
- `test-data/` — тестовые данные для разработки

## 🔧 Как использовать

### Для разработки:
1. Используйте файлы в `responses/` как моки для фронтенда
2. Валидируйте ответы с помощью схем в `schemas/`
3. Тестируйте API с помощью запросов в `requests/`

### Для тестирования:
```bash
# Тестирование с cURL
curl -X GET "https://api.petrushka.ru/api/v1/partners/shops?city=Москва"
